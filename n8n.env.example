# .env

# ----------------------------------
#         N8N SETTINGS
# ----------------------------------
# Set the timezone for n8n.
# I've set this to your current location.
TZ=Asia/Taipei
GENERIC_TIMEZONE=Asia/Taipei

# The encryption key is used to encrypt credential data in the database.
# IMPORTANT: Change this to a secure, random 32-character string.
# You can generate one easily by running: openssl rand -hex 16
N8N_ENCRYPTION_KEY=YourSecretRandomStringChangeThisNow!!!

# ----------------------------------
#      POSTGRESQL SETTINGS
# ----------------------------------
# These variables are used to initialize the PostgreSQL database.
# The n8n service will use the variables below to connect to it.
POSTGRES_DB=n8n
POSTGRES_USER=n8nuser

# IMPORTANT: Change this to a strong, unique password.
POSTGRES_PASSWORD=YourSuperSecurePasswordChangeThis

# ----------------------------------
#   N8N DATABASE CONNECTION CONFIG
# ----------------------------------
# These tell the n8n service how to connect to the postgres service.
# The values should reference the PostgreSQL settings above.
DB_TYPE=postgresdb
DB_POSTGRESDB_HOST=postgres
DB_POSTGRESDB_PORT=5432
DB_POSTGRESDB_DATABASE=${POSTGRES_DB}
DB_POSTGRESDB_USER=${POSTGRES_USER}
DB_POSTGRESDB_PASSWORD=${POSTGRES_PASSWORD}
